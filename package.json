{
  "name": "examnotes-ai",
  "version": "1.0.0",
  "description": "AI-Powered Exam Notes Generator",
  "scripts": {
    "install-all": "cd server && npm install && cd ../client && npm install --include=dev",
    "build": "cd client && npm run build",
    "start": "cd server && node index.js",
    "dev": "cd server && npm run dev"
  },
  "engines": {
    "node": "20"
  }
}
